/**
 * Created by harini on 11/12/17.
 // */
// Create a class. Initialize the required variables of the REST API

export class RestAPIVO{
 name: string;
 description:string;
 fooditems:Array<string>;
 imagepath:string;
 price:string;
}

// Create classes for item and category with required attributes
// Ensure to define only one class inside each ts
// Comment the details for all steps
// Note: every function should have a camelCase and class CaptCase
// Don't use underscore with class name and variable. Only for constants. Not for any variables or objects

